{
  "tasks": {
    "reinstall": "deno install --global --force --config deno.json --allow-read --allow-write scaffold-component.js",
    "clean": "rm -rf sample-component",
    "build-sample": "scaffold-component sample-component --force"
  }
}

